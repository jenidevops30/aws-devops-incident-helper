# Branch Workflow

This feature extends the existing Investigation Workspace branch and is intentionally kept in the same review branch for now.

Target review flow:

`feature/incident-runbook-generator` → `feature/incident-investigation-workspace` → validation → merge toward `main`.
